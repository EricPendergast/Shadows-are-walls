
public interface Lever {
    //public float GetPosition();
    // TODO: Maybe this should be a float, so that the lever can control the
    // rate at which it is pulled
    void MovePosition(int direction);
}
